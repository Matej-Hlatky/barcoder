{
  "name": "barcoder",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "node build.js && playwright test"
  },
  "dependencies": {
    "@bwip-js/browser": "4.11.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "vitest": "^3.0.0"
  }
}
